const assert = require('chai').assert

describe("characterCounter", function(){
  context("with a valid string", function(){
    it("returns a count for each character in a string", function(){
      assertEqua
    })
  })
})